import React from 'react'
import ReactDOM from 'react-dom'
import './style.css'

ReactDOM.render(
  <React.StrictMode>
    <div>初期テンプレート</div>
  </React.StrictMode>,
  document.getElementById('root')
)
